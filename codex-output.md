- Added a polished greeting page at `hello-from-codex.html:1` featuring a gradient backdrop, glassmorphism card, and CTA link back to `index.html`.
- Tests not run (static HTML change).

Optional next step:
1. Open `hello-from-codex.html` in a browser to confirm the styling looks as expected.